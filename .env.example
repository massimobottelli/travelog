# PostgreSQL connection
DATABASE_URL=postgresql://travelog:password@localhost:5432/travelog

# Root directory of the photo archive.
# The path is mounted from the NAS.
TRAVELOG_PHOTO_ROOT=/path/to/photo/archive
